# Roki CM4 board files

This directory will contain the boot partition configuration, custom DT
overlays, kernel firmware inputs, rootfs overlay and genimage configuration.
Only the initial kernel policy fragment is present until the boot image layout
and external Dinit package are added.
